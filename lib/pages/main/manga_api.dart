import 'package:flutter/material.dart';

class MangaApiData extends StatelessWidget {
  const MangaApiData({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}